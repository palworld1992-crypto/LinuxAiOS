pub struct BlockchainLightClient;

impl Default for BlockchainLightClient {
    fn default() -> Self {
        Self::new()
    }
}

impl BlockchainLightClient {
    pub fn new() -> Self {
        Self
    }
}
